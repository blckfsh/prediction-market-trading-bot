# Legacy Slug Rule Doc

The canonical rules playbook now lives at [`docs/crypto-bet-rules.md`](crypto-bet-rules.md).

This file remains in place as a compatibility pointer because legacy docs and chats may still reference `slug-match-rules.md`.

Use the new document for:
- primary `crypto-bet` endpoints
- `ACTIVE` / `INACTIVE` status behavior
- rollout examples and payloads

