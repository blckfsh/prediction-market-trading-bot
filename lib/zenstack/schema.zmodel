datasource db {
  provider = "postgresql"
}

enum TradeStatus {
  BOUGHT
  SOLD
}

model Trade {
  id                Int        @id @default(autoincrement())
  marketId          Int
  slug              String
  amount            Int
  transactionHash   String
  timestamp         DateTime
  status            TradeStatus
}

